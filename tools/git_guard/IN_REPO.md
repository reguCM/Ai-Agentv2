# In-repo Git Guard

Promoted from sandbox `goals/_git-guard/`.
Hooks must call `tools/git_guard/guard.py` via `git rev-parse --show-toplevel`.
The sandbox copy was a superseded mirror and has been retired.
