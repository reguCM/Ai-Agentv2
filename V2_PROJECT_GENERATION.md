# AI-Agent System V2

この Repository は **AI-Agent System V2** の正式開発基盤である。

初期状態は、旧 AI-Agent Repository で検証済みだった最新作業状態から、新しい独立 Repository として生成した。

旧 Repository・旧 Git 履歴・Recovery 資料は **LEGACY_EVIDENCE_ONLY** とする。

通常の仕様判断・実装判断・Runtime 判断・Fallback で旧構造を参照しない。

明示的な監査・Evidence 確認が必要な場合のみ参照する。

| Field | Value |
|-------|--------|
| `CURRENT_CANONICAL_GENERATION` | `V2` |
| `LEGACY_AUTOMATIC_FALLBACK` | `False` |
