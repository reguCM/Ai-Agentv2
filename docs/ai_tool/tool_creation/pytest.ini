[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    regression: Phase 2 regression parity tests
    fixture_check: output fixture structural checks
    real_web: Real public URL smoke test (network-dependent; not part of deterministic gate)
